# This is the default environment, used when you run gulp all; this can either
# be the same as the production environment or a separate dev environment
DB_HOST=YOUR_DB_HOST
DB_PORT=3306
DB_USER=YOUR_DB_USER
DB_PASS=YOUR_DB_PASS
DB_DATABASE=YOUR_DB
